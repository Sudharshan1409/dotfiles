# Create or add a window to the current group
bind = SUPER, G, togglegroup

# Switch to next window in group
bind = SUPER, TAB, changegroupactive, f

# Switch to previous window in group
bind = SUPER SHIFT, TAB, changegroupactive, b

# Ungroup all windows (break group)
bind = SUPER SHIFT, G, lockgroups, toggle


